# Home_work 
s1 = set()  # первое множество
s2 = set()  # второе множество

while True:
    try:
        num = int(input())  # считываем число
    except ValueError:  # если введена пустая строка, выходим из цикла
        break
    
    if num == -1:  
        if s1:
            s2 -= s1  # удаляем элементы, которые есть в первом множестве из второго
        else:
            s1, s2 = s2, s1  # меняем местами множества, если первое было пустым
    else:
        if num in s2:  # если элемент уже есть во втором множестве, удаляем его
            s2.remove(num)
        else:
            s1.add(num)  # добавляем элемент в первое множество

# выводим все элементы, которые остались в множестве
for num in s1:
    print(num)
