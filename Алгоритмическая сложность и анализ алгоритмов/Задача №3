# Алгоритм Евклида 
def gcd(a, b):
    """
    Находит наибольший общий делитель двух чисел
    """
    while b:
        a, b = b, a % b
    return a


def lcm_euclidean(a, b):
    """
    Находит наименьшее общее кратное двух чисел с помощью алгоритма Евклида
    """
    return (a * b) // gcd(a, b)

# метод перебора 
def lcm_brute_force(a, b):
    """
    Находит наименьшее общее кратное двух чисел перебором
    """
    for i in range(1, a * b + 1):
        if i % a == 0 and i % b == 0:
            return i

# использование НОК 
def lcm_formula(a, b):
    """
    Находит наименьшее общее кратное двух чисел с помощью формулы НОК(A, B) = (A * B) / НОД(A, B)
    """
    return (a * b) // gcd(a, b)

# из этих трех самым эффективным явялется алгоритм Евклида
